spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: boot
    producer:
      client-id: boot
    bootstrap-servers: ["192.168.1.204:9092"]
  application:
    name: collector
server:
  port: 8889


collector.buffer.size: 100000
#collector.spark.master.uri: spark://192.168.1.204:7077
#collector.spark.master.uri: yarn
---

spring:
  profiles:
    active: ${ACTIVE_PROFILE:dev}

